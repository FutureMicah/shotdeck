# Roadmap

- [x] Redesign the authenticated screenshot deck using the selected Neon Mint, Sora/Manrope, full-width direction
- [x] Add revocable per-admin or per-device ingestion tokens and private authenticated upload endpoint
- [ ] Add admin token management UI, setup guidance, and example client request
- [x] Add a credential-free screenshot ingestion endpoint that links uploads to the admin collection
- [ ] Test the credential-free endpoint end-to-end with a dummy PNG, confirm it appears in the dashboard, then delete the test image
- [ ] Verify build, private dashboard flows, endpoint validation, token ownership, and revocation
